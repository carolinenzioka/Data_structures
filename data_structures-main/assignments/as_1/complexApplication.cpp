
#include "complex.h"

int main()
  {
    // Instantiate Objects using all Constructors
    Complex (2.56, 4.87);
    Complex num2(8.92, 3.69);

    // Perform Operations
    num1.add(num2);
    num2.subtract(num2);
    num1.multiply(num2);
    num1.divide(num2);
    num1.modulus();

    return 0;
}
